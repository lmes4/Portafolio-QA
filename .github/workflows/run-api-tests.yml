name: Run API Tests - POSTMAN
on:
  push:  # Se ejecuta en cada push
    branches:
      - main
  workflow_dispatch: {}  # Permite ejecución manual
  schedule:
    - cron: '0 0 * * 1'  # Se ejecuta semanalmente. Los lunes a media noche

jobs:
  automated-api-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Postman CLI
        run: |
          curl -o- "https://dl-cli.pstmn.io/install/linux64.sh" | sh

      - name: Install Newman and HTML Reporter
        run: |
          npm install -g newman
          npm install -g newman-reporter-html

      - name: Download Postman collection using Postman API
        run: |
          curl --location --request GET "https://api.getpostman.com/collections/33801345-181c803c-a263-4988-9141-4482d833eb4c" \
          --header "X-Api-Key: ${{ secrets.POSTMAN_API_KEY }}" \
          --output collection.json

      - name: Download Postman environment using Postman API
        run: |
          curl --location --request GET "https://api.getpostman.com/environments/33801345-e08ef3e0-e06f-4b5c-936b-179df25cea75" \
          --header "X-Api-Key: ${{ secrets.POSTMAN_API_KEY }}" \
          --output environment.json

      - name: Run API tests with Newman and generate HTML report
        run: |
          newman run collection.json -e environment.json --reporters cli,html --reporter-html-export report.html

      - name: Configure Git for deployment
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'

      - name: Upload HTML report as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: postman-report
          path: report.html

      - name: Upload Postman collection as artifact
        uses: actions/upload-artifact@v4
        with:
          name: postman-collection
          path: collection.json
